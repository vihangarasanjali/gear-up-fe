import React from "react";

export const metadata = {
  title: "Customer Login - Gear Up",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
